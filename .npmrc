# Metro (React Native / Expo SDK 54) cannot resolve packages through pnpm's default
# isolated node_modules layout — Expo's documented fix for pnpm monorepos is a
# hoisted layout. Revisit when the app moves to an SDK with the new resolver (>= 57).
node-linker=hoisted
